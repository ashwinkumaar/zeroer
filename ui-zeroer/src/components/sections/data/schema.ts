import { TFormData, formDefaultValues, formSchema } from '@/components/sections/main';

export const filterSchema = formSchema;

export type TFilterSchema = TFormData;

export const filterDefaultValues = formDefaultValues;
